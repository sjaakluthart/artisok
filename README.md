# artisoks

## Setup

Install the required modules:

```bash
yarn install
```

## Development

Generate the HTML files:

```bash
make build
```

Watch for changes:

```bash
yarn run watch
```

Start browsersync:

```bash
yarn run start
```
